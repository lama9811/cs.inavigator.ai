{
  "_note": "Original Practice questions derived from CS Navigator's original Learn lesson for this topic.",
  "concept": "lists",
  "concept_label": "Lists / Arrays",
  "questions": [
    {
      "id": "lists-lesson-check-1",
      "concept": "lists",
      "difficulty": "easy",
      "kind": "mcq-behavior",
      "title": "Lists / Arrays practice 1",
      "variants": {
        "python": {
          "code": "colors = [\"red\", \"green\", \"blue\"]\nprint(colors[len(colors)])",
          "prompt": "What happens when this runs?",
          "choices": [
            "It raises an IndexError",
            "It prints blue",
            "It prints red",
            "It prints 3"
          ],
          "answer_index": 0,
          "explanation": "`len(colors)` is 3, so this asks for `colors[3]`. The valid indexes are 0, 1, and 2, so index 3 is one step past the end and Python raises an IndexError. If you picked \"blue\", you reasoned that the length points at the last item, which is the exact assumption that causes this error. To get \"blue\", subtract one: `colors[len(colors) - 1]`, or just write `colors[-1]`."
        },
        "java": {
          "code": "int[] scores = new int[3];\nSystem.out.println(scores[0]);\nSystem.out.println(scores.length);\nscores[3] = 99;\nSystem.out.println(\"done\");",
          "prompt": "Which of these describes what Java does with this code?",
          "choices": [
            "It prints 0 and 3, then throws an ArrayIndexOutOfBoundsException",
            "It prints 0, 3, and done, because assigning to scores[3] grows the array",
            "It refuses to compile, because slot 3 does not exist",
            "It prints nothing, because the array was never filled in"
          ],
          "answer_index": 0,
          "explanation": "The first two lines are fine: an int array starts full of zeros, so `scores[0]` is 0, and `scores.length` is 3. Then `scores[3] = 99` asks for slot 3 when the only slots are 0, 1, and 2. Java throws an ArrayIndexOutOfBoundsException and stops, so \"done\" never prints. The compile error is a tempting pick, but an index is a value the program works out while running, so Java cannot judge it in advance and only finds out when it arrives there. Assigning past the end never grows an array. The size was fixed the moment you wrote `new int[3]`."
        },
        "javascript": {
          "code": "const values=[4,8,12];",
          "prompt": "Which reads the final item?",
          "choices": [
            "values[values.length-1]",
            "values[values.length]",
            "values[-1]",
            "values.last"
          ],
          "answer_index": 0,
          "explanation": "Length is three while the final valid index is two, so subtract one."
        },
        "cpp": {
          "code": null,
          "prompt": "Which is the last valid index for size three?",
          "choices": [
            "2",
            "3",
            "1",
            "4"
          ],
          "answer_index": 0,
          "explanation": "Zero-based positions are zero, one, and two, so the count itself is outside the vector."
        }
      }
    },
    {
      "id": "lists-lesson-check-2",
      "concept": "lists",
      "difficulty": "easy",
      "kind": "mcq-behavior",
      "title": "Lists / Arrays practice 2",
      "variants": {
        "python": {
          "code": "nums = [1, 2, 2, 3, 2]\nfor n in nums:\n    if n == 2:\n        nums.remove(n)\nprint(nums)",
          "prompt": "This code is trying to delete every 2. It prints `[1, 3, 2]`. Why is a 2 still there?",
          "choices": [
            "Removing an item shifts the rest left, so the loop steps over the item that moved into the gap",
            "`.remove()` only ever deletes one item, no matter how many times you call it",
            "The loop ends early because the list got shorter than it started",
            "`.remove()` cannot delete numbers, only strings"
          ],
          "answer_index": 0,
          "explanation": "The loop keeps its place by position. It checks position 1, finds a 2, and removes it. Everything after that slides one step to the left, but the loop still advances to position 2, so the 2 that just moved into position 1 is never looked at. `.remove()` worked fine every time you called it, which is why the second choice is tempting and still wrong. The real problem is that you changed the list out from under the loop. Loop over the list to decide what to keep, and put those items in a new list."
        },
        "java": {
          "code": "ArrayList<String> names = new ArrayList<>();\nnames.add(\"Ada\");\nnames.add(\"Grace\");\n\n// which line prints 2?",
          "prompt": "`names` is an `ArrayList<String>` holding two names. Which line prints 2?",
          "choices": [
            "`System.out.println(names.size());`",
            "`System.out.println(names.length);`",
            "`System.out.println(names.length());`",
            "`System.out.println(len(names));`"
          ],
          "answer_index": 0,
          "explanation": "An ArrayList counts with `size()`. The tempting answer is `names.length`, because you just learned that arrays use `length` and an ArrayList feels like the same sort of thing. It is not, and the compiler says so: \"cannot find symbol: variable length\". `names.length()` fails for the same reason, since `length()` belongs to String. Sort them by what you are holding: array uses `length`, String uses `length()`, ArrayList uses `size()`. As for `len(names)`, that is Python, and Java will not recognize it at all."
        },
        "javascript": {
          "code": "const colors=[\"blue\"];\ncolors.push(\"orange\");\nconsole.log(colors.length);",
          "prompt": "What is printed?",
          "choices": [
            "2",
            "1",
            "orange",
            "a const error"
          ],
          "answer_index": 0,
          "explanation": "Push mutates the existing array, so const permits it and length becomes two."
        },
        "cpp": {
          "code": null,
          "prompt": "Which operation appends a value?",
          "choices": [
            "values.push_back(9);",
            "values.add(9);",
            "values[values.size()];",
            "append(values,9);"
          ],
          "answer_index": 0,
          "explanation": "Vector provides push_back to add one element at its end."
        }
      }
    },
    {
      "id": "lists-summary-01",
      "concept": "lists",
      "difficulty": "easy",
      "kind": "mcq-behavior",
      "title": "Lists / Arrays summary",
      "variants": {
        "python": {
          "code": null,
          "prompt": "Which statement best describes Lists / Arrays?",
          "choices": [
            "Hold many values under one name, in order.",
            "It is used only to print a message.",
            "It automatically fixes every program error.",
            "It removes the need for variables and tests."
          ],
          "answer_index": 0,
          "explanation": "A list holds several values in order, written in square brackets: `scores = [88, 92, 75]`. You reach an item by its position, and positions start at 0, so `scores[0]` is the first one. `len(scores)` tells you how many items there are. Because counting starts at 0, the last valid position is always one less than the length. Lists can change after you make them: `.append(x)` adds to the end, `.pop()` removes the last item, and `.remove(x)` removes the first item equal to x."
        },
        "java": {
          "code": null,
          "prompt": "Which statement best describes Lists / Arrays?",
          "choices": [
            "Hold many values under one name: a fixed array, or a growable ArrayList.",
            "It is used only to print a message.",
            "It automatically fixes every program error.",
            "It removes the need for variables and tests."
          ],
          "answer_index": 0,
          "explanation": "An array holds a fixed number of values of one type: `int[] nums = new int[5];` reserves exactly 5 slots, and that size never changes. Positions start at 0, so the last valid index is `nums.length - 1`. On an array, `length` is a field, so it takes no parentheses. When you do not know the count ahead of time, use an `ArrayList`, which grows as you add to it: `add` puts an item on the end, `get(i)` reads one, `size()` counts them. An ArrayList holds objects, so a list of whole numbers is `ArrayList<Integer>`, never `ArrayList<int>`."
        },
        "javascript": {
          "code": null,
          "prompt": "Which statement best describes Lists / Arrays?",
          "choices": [
            "Store ordered values and access them by index.",
            "It is used only to print a message.",
            "It automatically fixes every program error.",
            "It removes the need for variables and tests."
          ],
          "answer_index": 0,
          "explanation": "Arrays hold ordered values with zero-based indexes. `.length` gives the count, `.push(value)` adds at the end, and `.pop()` removes the final item."
        },
        "cpp": {
          "code": null,
          "prompt": "Which statement best describes Lists / Arrays?",
          "choices": [
            "Store ordered elements in arrays and vectors.",
            "It is used only to print a message.",
            "It automatically fixes every program error.",
            "It removes the need for variables and tests."
          ],
          "answer_index": 0,
          "explanation": "A fixed array has a compile-time size, while `std::vector` can grow. Vector indexes start at zero, `.size()` returns the count, and `.push_back` appends."
        }
      }
    },
    {
      "id": "lists-supplement-01",
      "concept": "lists",
      "difficulty": "easy",
      "kind": "mcq-behavior",
      "title": "Lists / Arrays practice 5",
      "variants": {
        "python": {
          "code": "nums = [1, 2, 2, 3, 2]\nfor n in nums:\n    if n == 2:\n        nums.remove(n)\nprint(nums)",
          "prompt": "What does this print, and why?",
          "choices": [
            "[1, 3, 2] — removing while looping makes the loop skip items",
            "[1, 3] — every 2 is removed",
            "[1, 2, 2, 3, 2] — remove does nothing inside a loop",
            "It raises an error"
          ],
          "answer_index": 0,
          "explanation": "Deleting from a list while looping over it shifts later items back by one, and the loop's position still moves forward, so it steps past the item that slid into the gap. Two of the 2s get skipped and survive, leaving [1, 3, 2]. Build a new list instead of mutating the one you are iterating."
        },
        "java": {
          "code": "int[] arr = {4, 7, 2};\nfor (int i = 0; i <= arr.length; i++) {\n    System.out.println(arr[i]);\n}",
          "prompt": "What does this loop do?",
          "choices": [
            "Prints 4, 7, 2, then throws ArrayIndexOutOfBoundsException at i = 3",
            "Prints 4, 7, 2 and stops cleanly",
            "Prints 4, 7, 2, 0",
            "Fails to compile"
          ],
          "answer_index": 0,
          "explanation": "`<=` lets `i` reach 3, but an array of length 3 has valid indexes 0, 1, 2. `arr[3]` was never allocated, so after printing all three items Java throws ArrayIndexOutOfBoundsException. The length is always one past the last index, so the correct guard is `i < arr.length`."
        },
        "javascript": {
          "code": "const planned = [...courses];\nplanned.push(\"CS 301\");",
          "prompt": "After this runs, what is in the original `courses` array?",
          "choices": [
            "Unchanged — planned is a separate copy",
            "It also gains \"CS 301\"",
            "It becomes empty",
            "It throws, because arrays cannot be spread"
          ],
          "answer_index": 0,
          "explanation": "`[...courses]` builds a new array with the same items, so `planned` and `courses` are independent. Pushing to `planned` does not touch `courses`. Assigning `const planned = courses` (no spread) would share one array, and then the push would show up in both."
        },
        "cpp": {
          "code": "int value = scores.at(index);",
          "prompt": "How does `scores.at(index)` differ from `scores[index]` when `index` is out of range?",
          "choices": [
            "at() throws std::out_of_range; [] is undefined behavior",
            "They behave identically",
            "at() returns 0; [] returns -1",
            "at() is invalid syntax on a vector"
          ],
          "answer_index": 0,
          "explanation": "`at()` checks the bound and throws `std::out_of_range` on a bad index, so the failure is visible. `operator[]` does not check and reads invalid memory, which is undefined behavior and may not crash right away. Prefer `at()` when the index might be out of range."
        }
      }
    },
    {
      "id": "lists-authored-01",
      "concept": "lists",
      "difficulty": "medium",
      "kind": "mcq-behavior",
      "title": "Lists / Arrays concept check 1",
      "variants": {
        "python": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 1",
          "code": null,
          "prompt": "A list has 4 items. Which statement about its indexes is true?",
          "choices": [
            "A list with 4 items has a length of 4, but its last index is 3. Asking for `scores[4]` reaches past the end and Python stops with an IndexError.",
            "Use the length itself as the final valid index.",
            "Remove items from the same list while a loop is walking through it.",
            "Assume arrays automatically grow when assigning past the end."
          ],
          "answer_index": 0,
          "explanation": "A list holds several values in order, written in square brackets: `scores = [88, 92, 75]`. You reach an item by its position, and positions start at 0, so `scores[0]` is the first one. That reasoning supports the answer: A list with 4 items has a length of 4, but its last index is 3. Asking for `scores[4]` reaches past the end and Python stops with an IndexError."
        },
        "java": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 1",
          "code": null,
          "prompt": "How do you read the length of a Java array?",
          "choices": [
            "Write `scores.length`. Write `scores.length()` and the compiler stops with \"cannot find symbol: method length()\".",
            "Use the length itself as the final valid index.",
            "Remove items from the same list while a loop is walking through it.",
            "Assume arrays automatically grow when assigning past the end."
          ],
          "answer_index": 0,
          "explanation": "An array holds a fixed number of values of one type: `int[] nums = new int[5];` reserves exactly 5 slots, and that size never changes. Positions start at 0, so the last valid index is `nums.length - 1`. That reasoning supports the answer: Write `scores.length`. Write `scores.length()` and the compiler stops with \"cannot find symbol: method length()\"."
        },
        "javascript": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 1",
          "code": null,
          "prompt": "Which boundary detail prevents this kind of off-by-one or conversion bug?",
          "choices": [
            "Const prevents assigning a different array to the variable, but it does not freeze the current array contents.",
            "Use the length itself as the final valid index.",
            "Remove items from the same list while a loop is walking through it.",
            "Assume arrays automatically grow when assigning past the end."
          ],
          "answer_index": 0,
          "explanation": "Arrays hold ordered values with zero-based indexes. `.length` gives the count, `.push(value)` adds at the end, and `.pop()` removes the final item. That reasoning supports the answer: Const prevents assigning a different array to the variable, but it does not freeze the current array contents."
        },
        "cpp": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 1",
          "code": null,
          "prompt": "Which statement correctly follows the data through this operation?",
          "choices": [
            "Unchecked bracket access past the end causes undefined behavior. Use valid index bounds, or `.at()` when you want an exception for an invalid position.",
            "Use the length itself as the final valid index.",
            "Remove items from the same list while a loop is walking through it.",
            "Assume arrays automatically grow when assigning past the end."
          ],
          "answer_index": 0,
          "explanation": "A fixed array has a compile-time size, while `std::vector` can grow. Vector indexes start at zero, `.size()` returns the count, and `.push_back` appends. That reasoning supports the answer: Unchecked bracket access past the end causes undefined behavior. Use valid index bounds, or `.at()` when you want an exception for an invalid position."
        }
      }
    },
    {
      "id": "lists-authored-02",
      "concept": "lists",
      "difficulty": "medium",
      "kind": "mcq-behavior",
      "title": "Lists / Arrays concept check 2",
      "variants": {
        "python": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 2",
          "code": null,
          "prompt": "In Python list code, how are `remove` and `pop` different?",
          "choices": [
            "Use a value-removal method when the code needs to remove by position.",
            "The two are easy to mix up. `tasks.remove(\"code\")` looks for the item \"code\" and deletes the first one it finds.",
            "Use an index-removal method when the code needs to remove by value.",
            "Treat fixed arrays and growable lists as if their APIs are identical."
          ],
          "answer_index": 1,
          "explanation": "A list holds several values in order, written in square brackets: `scores = [88, 92, 75]`. You reach an item by its position, and positions start at 0, so `scores[0]` is the first one. That reasoning supports the answer: The two are easy to mix up. `tasks.remove(\"code\")` looks for the item \"code\" and deletes the first one it finds."
        },
        "java": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 2",
          "code": null,
          "prompt": "Why does Java require `ArrayList<Integer>` instead of `ArrayList<int>`?",
          "choices": [
            "Use a value-removal method when the code needs to remove by position.",
            "An ArrayList can only hold objects, and `int` is not one. It is a primitive: a plain machine number with no methods attached.",
            "Use an index-removal method when the code needs to remove by value.",
            "Treat fixed arrays and growable lists as if their APIs are identical."
          ],
          "answer_index": 1,
          "explanation": "An array holds a fixed number of values of one type: `int[] nums = new int[5];` reserves exactly 5 slots, and that size never changes. Positions start at 0, so the last valid index is `nums.length - 1`. That reasoning supports the answer: An ArrayList can only hold objects, and `int` is not one. It is a primitive: a plain machine number with no methods attached."
        },
        "javascript": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 2",
          "code": null,
          "prompt": "Which choice describes a reliable Lists / Arrays practice?",
          "choices": [
            "Use a value-removal method when the code needs to remove by position.",
            "Begin indexing at zero.",
            "Use an index-removal method when the code needs to remove by value.",
            "Treat fixed arrays and growable lists as if their APIs are identical."
          ],
          "answer_index": 1,
          "explanation": "Arrays hold ordered values with zero-based indexes. `.length` gives the count, `.push(value)` adds at the end, and `.pop()` removes the final item. That reasoning supports the answer: Begin indexing at zero."
        },
        "cpp": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 2",
          "code": null,
          "prompt": "Which choice describes a reliable Lists / Arrays practice?",
          "choices": [
            "Use a value-removal method when the code needs to remove by position.",
            "Include vector.",
            "Use an index-removal method when the code needs to remove by value.",
            "Treat fixed arrays and growable lists as if their APIs are identical."
          ],
          "answer_index": 1,
          "explanation": "A fixed array has a compile-time size, while `std::vector` can grow. Vector indexes start at zero, `.size()` returns the count, and `.push_back` appends. That reasoning supports the answer: Include vector."
        }
      }
    },
    {
      "id": "lists-authored-03",
      "concept": "lists",
      "difficulty": "medium",
      "kind": "mcq-behavior",
      "title": "Lists / Arrays concept check 3",
      "variants": {
        "python": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 3",
          "code": null,
          "prompt": "A list result is missing neighboring items or is off by one. What should you check first?",
          "choices": [
            "Read an item before checking whether the collection is empty.",
            "Skip the last item because the loop stops one position too early.",
            "A list that comes out one item short, or one item long, or missing exactly the values that sat next to each other, is almost always one of two bugs. Either an index is off by one, or you changed the list while looping over it.",
            "Visit one extra position because the loop uses `<=` with the length."
          ],
          "answer_index": 2,
          "explanation": "A list holds several values in order, written in square brackets: `scores = [88, 92, 75]`. You reach an item by its position, and positions start at 0, so `scores[0]` is the first one. That reasoning supports the answer: A list that comes out one item short, or one item long, or missing exactly the values that sat next to each other, is almost always one of two bugs. Either an index is off by one, or you changed the list while looping over it."
        },
        "java": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 3",
          "code": null,
          "prompt": "Which statement about Java compile errors is true?",
          "choices": [
            "Read an item before checking whether the collection is empty.",
            "Skip the last item because the loop stops one position too early.",
            "Java checks types while it builds your program, so `ArrayList<int>` and `nums.length()` are caught before a single line executes. Nothing you wrote ever ran.",
            "Visit one extra position because the loop uses `<=` with the length."
          ],
          "answer_index": 2,
          "explanation": "An array holds a fixed number of values of one type: `int[] nums = new int[5];` reserves exactly 5 slots, and that size never changes. Positions start at 0, so the last valid index is `nums.length - 1`. That reasoning supports the answer: Java checks types while it builds your program, so `ArrayList<int>` and `nums.length()` are caught before a single line executes. Nothing you wrote ever ran."
        },
        "javascript": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 3",
          "code": null,
          "prompt": "Which recommendation belongs in a review of Lists / Arrays code?",
          "choices": [
            "Read an item before checking whether the collection is empty.",
            "Skip the last item because the loop stops one position too early.",
            "Subtract one for the end.",
            "Visit one extra position because the loop uses `<=` with the length."
          ],
          "answer_index": 2,
          "explanation": "Arrays hold ordered values with zero-based indexes. `.length` gives the count, `.push(value)` adds at the end, and `.pop()` removes the final item. That reasoning supports the answer: Subtract one for the end."
        },
        "cpp": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 3",
          "code": null,
          "prompt": "Which recommendation belongs in a review of Lists / Arrays code?",
          "choices": [
            "Read an item before checking whether the collection is empty.",
            "Skip the last item because the loop stops one position too early.",
            "Begin at zero.",
            "Visit one extra position because the loop uses `<=` with the length."
          ],
          "answer_index": 2,
          "explanation": "A fixed array has a compile-time size, while `std::vector` can grow. Vector indexes start at zero, `.size()` returns the count, and `.push_back` appends. That reasoning supports the answer: Begin at zero."
        }
      }
    },
    {
      "id": "lists-authored-04",
      "concept": "lists",
      "difficulty": "medium",
      "kind": "mcq-behavior",
      "title": "Lists / Arrays concept check 4",
      "variants": {
        "python": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 4",
          "code": null,
          "prompt": "Which guideline helps prevent a common Lists / Arrays mistake?",
          "choices": [
            "Copy only the variable name and assume a fresh list was created.",
            "Mutate shared list data when independent plans are needed.",
            "Sort the list before preserving the original order.",
            "The first item is at index 0, and the last is at `len(lst) - 1`. Use `lst[-1]` and skip the arithmetic."
          ],
          "answer_index": 3,
          "explanation": "A list holds several values in order, written in square brackets: `scores = [88, 92, 75]`. You reach an item by its position, and positions start at 0, so `scores[0]` is the first one. That reasoning supports the answer: The first item is at index 0, and the last is at `len(lst) - 1`. Use `lst[-1]` and skip the arithmetic."
        },
        "java": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 4",
          "code": null,
          "prompt": "What does `nums.remove(1)` do on an `ArrayList<Integer>`?",
          "choices": [
            "Copy only the variable name and assume a fresh list was created.",
            "Mutate shared list data when independent plans are needed.",
            "Sort the list before preserving the original order.",
            "For a list of Integers, `nums.remove(1)` deletes the item at position 1, not the item whose value is 1. Java reads a plain `int` as an index."
          ],
          "answer_index": 3,
          "explanation": "An array holds a fixed number of values of one type: `int[] nums = new int[5];` reserves exactly 5 slots, and that size never changes. Positions start at 0, so the last valid index is `nums.length - 1`. That reasoning supports the answer: For a list of Integers, `nums.remove(1)` deletes the item at position 1, not the item whose value is 1. Java reads a plain `int` as an index."
        },
        "javascript": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 4",
          "code": null,
          "prompt": "Which guideline helps prevent a common Lists / Arrays mistake?",
          "choices": [
            "Copy only the variable name and assume a fresh list was created.",
            "Mutate shared list data when independent plans are needed.",
            "Sort the list before preserving the original order.",
            "Use push and pop deliberately."
          ],
          "answer_index": 3,
          "explanation": "Arrays hold ordered values with zero-based indexes. `.length` gives the count, `.push(value)` adds at the end, and `.pop()` removes the final item. That reasoning supports the answer: Use push and pop deliberately."
        },
        "cpp": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 4",
          "code": null,
          "prompt": "Which guideline helps prevent a common Lists / Arrays mistake?",
          "choices": [
            "Copy only the variable name and assume a fresh list was created.",
            "Mutate shared list data when independent plans are needed.",
            "Sort the list before preserving the original order.",
            "Stay below size."
          ],
          "answer_index": 3,
          "explanation": "A fixed array has a compile-time size, while `std::vector` can grow. Vector indexes start at zero, `.size()` returns the count, and `.push_back` appends. That reasoning supports the answer: Stay below size."
        }
      }
    },
    {
      "id": "lists-authored-05",
      "concept": "lists",
      "difficulty": "medium",
      "kind": "mcq-behavior",
      "title": "Lists / Arrays concept check 5",
      "variants": {
        "python": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 5",
          "code": null,
          "prompt": "A classmate is checking their understanding of Lists / Arrays. Which advice is correct?",
          "choices": [
            "Loop with `for item in lst:` when you only need the values. A loop that never touches an index has no index to get wrong.",
            "Use nested indexes without checking the inner list length.",
            "Assume every list method returns the changed list.",
            "Append to the wrong list while building a result."
          ],
          "answer_index": 0,
          "explanation": "A list holds several values in order, written in square brackets: `scores = [88, 92, 75]`. You reach an item by its position, and positions start at 0, so `scores[0]` is the first one. That reasoning supports the answer: Loop with `for item in lst:` when you only need the values. A loop that never touches an index has no index to get wrong."
        },
        "java": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 5",
          "code": null,
          "prompt": "A classmate is checking their understanding of Lists / Arrays. Which advice is correct?",
          "choices": [
            "The first index is 0 and the last is `length - 1`. Loop with `i < arr.length`, never `i <= arr.length`.",
            "Use nested indexes without checking the inner list length.",
            "Assume every list method returns the changed list.",
            "Append to the wrong list while building a result."
          ],
          "answer_index": 0,
          "explanation": "An array holds a fixed number of values of one type: `int[] nums = new int[5];` reserves exactly 5 slots, and that size never changes. Positions start at 0, so the last valid index is `nums.length - 1`. That reasoning supports the answer: The first index is 0 and the last is `length - 1`. Loop with `i < arr.length`, never `i <= arr.length`."
        },
        "javascript": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 5",
          "code": null,
          "prompt": "A classmate is checking their understanding of Lists / Arrays. Which advice is correct?",
          "choices": [
            "Know which methods mutate.",
            "Use nested indexes without checking the inner list length.",
            "Assume every list method returns the changed list.",
            "Append to the wrong list while building a result."
          ],
          "answer_index": 0,
          "explanation": "Arrays hold ordered values with zero-based indexes. `.length` gives the count, `.push(value)` adds at the end, and `.pop()` removes the final item. That reasoning supports the answer: Know which methods mutate."
        },
        "cpp": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 5",
          "code": null,
          "prompt": "A classmate is checking their understanding of Lists / Arrays. Which advice is correct?",
          "choices": [
            "Choose checked access when useful.",
            "Use nested indexes without checking the inner list length.",
            "Assume every list method returns the changed list.",
            "Append to the wrong list while building a result."
          ],
          "answer_index": 0,
          "explanation": "A fixed array has a compile-time size, while `std::vector` can grow. Vector indexes start at zero, `.size()` returns the count, and `.push_back` appends. That reasoning supports the answer: Choose checked access when useful."
        }
      }
    },
    {
      "id": "lists-authored-06",
      "concept": "lists",
      "difficulty": "medium",
      "kind": "mcq-behavior",
      "title": "Lists / Arrays concept check 6",
      "variants": {
        "python": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 6",
          "code": null,
          "prompt": "Which choice shows sound reasoning about Lists / Arrays?",
          "choices": [
            "Reset the result list inside the loop.",
            "Avoid adding to or removing from a list while you loop over it. Build a new list of what you want to keep.",
            "Use the item value as if it were the item index.",
            "Ignore whether the data structure is fixed-size or growable."
          ],
          "answer_index": 1,
          "explanation": "A list holds several values in order, written in square brackets: `scores = [88, 92, 75]`. You reach an item by its position, and positions start at 0, so `scores[0]` is the first one. That reasoning supports the answer: Avoid adding to or removing from a list while you loop over it. Build a new list of what you want to keep."
        },
        "java": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 6",
          "code": null,
          "prompt": "Which choice shows sound reasoning about Lists / Arrays?",
          "choices": [
            "Reset the result list inside the loop.",
            "`arr[arr.length]` is out of bounds for every array, always. For the final item, ask for `arr[arr.length - 1]`.",
            "Use the item value as if it were the item index.",
            "Ignore whether the data structure is fixed-size or growable."
          ],
          "answer_index": 1,
          "explanation": "An array holds a fixed number of values of one type: `int[] nums = new int[5];` reserves exactly 5 slots, and that size never changes. Positions start at 0, so the last valid index is `nums.length - 1`. That reasoning supports the answer: `arr[arr.length]` is out of bounds for every array, always. For the final item, ask for `arr[arr.length - 1]`."
        },
        "javascript": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 6",
          "code": null,
          "prompt": "What is the main idea behind Lists / Arrays?",
          "choices": [
            "Reset the result list inside the loop.",
            "Store ordered values and access them by index.",
            "Use the item value as if it were the item index.",
            "Ignore whether the data structure is fixed-size or growable."
          ],
          "answer_index": 1,
          "explanation": "Arrays hold ordered values with zero-based indexes. `.length` gives the count, `.push(value)` adds at the end, and `.pop()` removes the final item. That reasoning supports the answer: Store ordered values and access them by index."
        },
        "cpp": {
          "difficulty": "medium",
          "kind": "mcq-behavior",
          "title": "Lists / Arrays concept check 6",
          "code": null,
          "prompt": "What is the main idea behind Lists / Arrays?",
          "choices": [
            "Reset the result list inside the loop.",
            "Store ordered elements in arrays and vectors.",
            "Use the item value as if it were the item index.",
            "Ignore whether the data structure is fixed-size or growable."
          ],
          "answer_index": 1,
          "explanation": "A fixed array has a compile-time size, while `std::vector` can grow. Vector indexes start at zero, `.size()` returns the count, and `.push_back` appends. That reasoning supports the answer: Store ordered elements in arrays and vectors."
        }
      }
    }
  ]
}
