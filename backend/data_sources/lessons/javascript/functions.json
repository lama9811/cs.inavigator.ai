{
  "_note": "ORIGINAL content. javascript.info and Eloquent JavaScript informed topic coverage only; no prose, examples, or exercises were copied.",
  "title": "Functions",
  "summary": "Package reusable behavior with inputs and returned results.",
  "minutes": 14,
  "error_classes": [
    "wrong_answer",
    "runtime",
    "syntax"
  ],
  "refresher": "**Parameters** receive arguments, and `return` sends a result to the caller. Local variables stay inside their call. Braced arrow **functions** need an explicit **return**.",
  "refresher_code": "const square=n=>n*n;\nconsole.log(square(4));",
  "_beginner_expanded": true,
  "sections": [
    {
      "id": "parameters-and-calls",
      "title": "Parameters and calls",
      "summary": "Package behavior and pass **arguments** into **parameters**.",
      "blocks": [
        {
          "kind": "text",
          "body": "**Parameters** receive arguments, and `return` sends a result to the caller. Local variables stay inside their call. Braced arrow **functions** need an explicit **return**. Package reusable behavior with inputs and returned results."
        },
        {
          "kind": "code",
          "caption": "Before real syntax, learn the shape: a **function** has a name, inputs, a body, and sometimes a returned result. The examples after this show the exact syntax.",
          "code": "function name(parameters):\n    body that does the job\n    return the result\n\ncall function name(arguments)"
        },
        {
          "kind": "visual",
          "title": "Follow a function call",
          "concept": "functions",
          "caption": "See arguments enter a function, a local variable appear, and return send the answer back.",
          "initial_state": {},
          "steps": [
            {
              "title": "Call with arguments",
              "body": "The caller sends 3 and 5 into the function.",
              "state": {
                "call_stack": [
                  "main: result = add(3, 5)"
                ],
                "active_call": 0,
                "note": "waiting for add"
              }
            },
            {
              "title": "Parameters receive values",
              "body": "Inside the function, a is 3 and b is 5.",
              "animation": "add",
              "state": {
                "call_stack": [
                  "main",
                  "add: a=3, b=5"
                ],
                "active_call": 1,
                "table": [
                  {
                    "key": "a",
                    "value": "3",
                    "active": true
                  },
                  {
                    "key": "b",
                    "value": "5",
                    "active": true
                  }
                ]
              }
            },
            {
              "title": "Return",
              "body": "Return sends 8 back to the caller, and the function call is finished.",
              "animation": "return",
              "state": {
                "call_stack": [
                  "main: result = 8"
                ],
                "active_call": 0,
                "note": "result stores 8"
              }
            }
          ]
        },
        {
          "kind": "text",
          "body": "Calling a function creates a local workspace for its **parameters** and variables. **Arguments** fill the parameters, statements run in order, and `return` sends one result back while ending that call. If execution reaches the closing brace without **return**, the result is `undefined`."
        },
        {
          "kind": "code",
          "caption": "The **arguments** fill two **parameters**, and **return** supplies the finished label.",
          "code": "function label(code,section){\n  return `${code}-${section}`;\n}\nconsole.log(label(\"CS101\",2));",
          "output": "CS101-2"
        }
      ]
    },
    {
      "id": "scope-and-reuse",
      "title": "Each call gets its own local names",
      "summary": "**Parameters** and temporary variables belong to one function call.",
      "blocks": [
        {
          "kind": "text",
          "body": "A function call creates a small local workspace. The **parameters** hold this call's arguments, and temporary variables made inside the function do not become global names."
        },
        {
          "kind": "code",
          "caption": "The two calls reuse the same parameter name, but each call receives a different value.",
          "code": "function tag(course) {\n  const label = \"Course: \" + course;\n  return label;\n}\n\nconsole.log(tag(\"CS101\"));\nconsole.log(tag(\"CS102\"));",
          "output": "Course: CS101\nCourse: CS102"
        },
        {
          "kind": "check",
          "prompt": "Why can both calls use the name `course` safely?",
          "code": "tag(\"CS101\")\ntag(\"CS102\")",
          "choices": [
            "Each call gets its own parameter value",
            "The second call deletes the function",
            "Parameters are shared across all calls",
            "The name must match the printed text"
          ],
          "answer_index": 0,
          "why": "The parameter name is local to the function call, so each call fills it with the argument passed that time."
        }
      ]
    },
    {
      "id": "return-vs-console",
      "title": "Return vs console.log",
      "summary": "**Return** values when later code needs the result.",
      "blocks": [
        {
          "kind": "compare",
          "caption": "**Return** a calculation when another part of the program needs it.",
          "wrong_label": "Common mistake",
          "wrong": "function total(a, b) {\n  console.log(a + b);\n}\nconst result = total(2, 3);",
          "right_label": "Clearer version",
          "right": "function total(a, b) {\n  return a + b;\n}\nconst result = total(2, 3);"
        },
        {
          "kind": "text",
          "body": "In the corrected call, `a` receives two and `b` receives three. The expression produces five, **return** hands five to the assignment, and `result` stores it. Printing can happen later without tying the **function** to one display choice."
        },
        {
          "kind": "callout",
          "tone": "mistake",
          "title": "Check the boundary",
          "body": "Console logging displays a **value** but does not **return** it; a **function** without return evaluates to `undefined`."
        },
        {
          "kind": "check",
          "prompt": "What is stored in answer?",
          "code": "function add(a,b){console.log(a+b);}\nconst answer=add(2,3);",
          "choices": [
            "undefined",
            "5",
            "\"5\"",
            "null"
          ],
          "answer_index": 0,
          "why": "The **function** prints five but reaches its end without returning a **value**."
        }
      ]
    },
    {
      "id": "return-paths-and-booleans",
      "title": "Return paths and boolean helpers",
      "summary": "A helper can **return** true or false so another decision can use it.",
      "blocks": [
        {
          "kind": "text",
          "body": "Many beginner functions answer a yes-or-no question. That answer should usually be returned, so an `if` statement or another function can use it."
        },
        {
          "kind": "code",
          "caption": "The helper returns a boolean. The caller decides what to print.",
          "code": "function isPassing(score) {\n  return score >= 70;\n}\n\nif (isPassing(82)) {\n  console.log(\"ready\");\n}",
          "output": "ready"
        },
        {
          "kind": "compare",
          "caption": "Printing inside the helper makes it harder to reuse in another condition.",
          "wrong_label": "Displays only",
          "wrong": "function isPassing(score) {\n  console.log(score >= 70);\n}",
          "right_label": "Returns a boolean",
          "right": "function isPassing(score) {\n  return score >= 70;\n}"
        },
        {
          "kind": "check",
          "prompt": "What should a yes-or-no helper return?",
          "code": "function isPassing(score) { ... }",
          "choices": [
            "a boolean value",
            "the function name",
            "a new console",
            "the parameter list"
          ],
          "answer_index": 0,
          "why": "A boolean result lets later code choose a branch from the helper answer."
        }
      ]
    },
    {
      "id": "arrow-functions-and-checklist",
      "title": "Arrow functions and checklist",
      "summary": "Watch braced arrow returns and test **edge** **arguments**.",
      "blocks": [
        {
          "kind": "check",
          "prompt": "Which arrow doubles **input**?",
          "code": "",
          "choices": [
            "const double=n=>n*2;",
            "const double=n=>{n*2;};",
            "const double=>n*2;",
            "const double=return n*2;"
          ],
          "answer_index": 0,
          "why": "The expression-bodied arrow returns automatically; the braced choice omits **return**."
        },
        {
          "kind": "list",
          "title": "Functions checklist",
          "items": [
            "Define clear **parameters**.",
            "**Return** needed data.",
            "Keep temporary names local.",
            "Test **edge** **arguments**."
          ]
        }
      ]
    }
  ],
  "learn_cards": {
    "call-01": {
      "title": "Definition is not execution",
      "body": "A function or method body waits until code calls it.",
      "notice": "Find the actual call before expecting the body to run.",
      "mistake": "Do not count a function body just because it appears above the output line.",
      "section_id": "parameters-and-calls",
      "example": "double(5)"
    },
    "print-not-return-01": {
      "title": "Printing is not returning",
      "body": "Printing shows a value to the person. Returning hands a value back to the program.",
      "notice": "Check whether the function has a return value or only output side effects.",
      "mistake": "Do not store the result of a no-return function and expect a useful value instead of undefined.",
      "section_id": "return-vs-console",
      "example": "function double(n) {\n  return n * 2;\n}"
    },
    "return-value-01": {
      "title": "Returned values replace the call",
      "body": "A function call with a return value can be used inside a larger expression.",
      "notice": "Evaluate each call, replace it with its returned value, then finish the expression.",
      "mistake": "Do not ignore the returned value when the next line uses it.",
      "section_id": "return-vs-console",
      "example": "double(5)"
    },
    "return-stops-01": {
      "title": "Return exits immediately",
      "body": "Once a return statement runs, the rest of that function call is over.",
      "notice": "After an early return, skip later lines in the same function call.",
      "mistake": "Do not keep executing a function after it has already returned.",
      "section_id": "return-vs-console",
      "example": "return value"
    },
    "missing-return-01": {
      "title": "Every path needs a return plan",
      "body": "If one branch returns but another branch reaches the end, the function may hand back no useful value.",
      "notice": "Trace the path for the specific input, not only the branch that looks intended.",
      "mistake": "Do not assume a skipped branch magically returns the other argument.",
      "section_id": "return-vs-console",
      "example": "if condition: return value"
    },
    "arg-order-01": {
      "title": "Arguments fill parameters by position",
      "body": "The first argument goes into the first parameter, the second into the second, and so on unless named arguments are used.",
      "notice": "Write the parameter values before evaluating the body.",
      "mistake": "Do not swap argument meaning because the names would sound nicer the other way.",
      "section_id": "parameters-and-calls",
      "example": "subtract(a, b)"
    },
    "local-scope-01": {
      "title": "Local variables stay inside",
      "body": "A value created inside a function or method is not automatically available outside it.",
      "notice": "Use return values to move information out of a function.",
      "mistake": "Do not print an inside-only name after the function has finished.",
      "section_id": "scope-and-reuse",
      "example": "function double(n) {\n  return n * 2;\n}"
    },
    "reuse-01": {
      "title": "Each call runs fresh",
      "body": "Calling the same function more than once applies the same rule to new arguments.",
      "notice": "Evaluate each call separately, then combine the returned values.",
      "mistake": "Do not reuse the previous call's local parameter values.",
      "section_id": "scope-and-reuse",
      "example": "double(5)"
    },
    "function-in-condition-01": {
      "title": "Boolean functions can drive decisions",
      "body": "A function that returns true or false can be used directly as a condition.",
      "notice": "Evaluate the returned boolean before choosing the branch.",
      "mistake": "Do not print the boolean if the code uses it to choose an if or else path.",
      "section_id": "return-paths-and-booleans",
      "example": "is_even(7)"
    },
    "param-copy-01": {
      "title": "Changing a parameter may not change the caller",
      "body": "A parameter is the function's local name for the value it received.",
      "notice": "If the function returns an updated value, the caller must store that return to keep it.",
      "mistake": "Do not assume a local parameter assignment rewrites the original variable.",
      "section_id": "scope-and-reuse",
      "example": "x = bump(x)"
    }
  }
}
