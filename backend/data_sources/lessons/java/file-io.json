{
  "_note": "ORIGINAL content. Topic coverage follows David Eck's Introduction to Programming Using Java. No prose, examples, or exercises were copied; the source guides curriculum order only.",
  "title": "File I/O",
  "summary": "Save and load text while handling Java's checked I/O failures.",
  "minutes": 11,
  "error_classes": [
    "runtime",
    "syntax",
    "wrong_answer"
  ],
  "refresher": "Use `Path` with `Files.readString` and `Files.writeString` for straightforward text files. File methods can throw IOException, so catch it where recovery is possible or declare it with `throws`. Choose UTF-8 and be clear about replacing versus appending.",
  "refresher_code": "Path path = Path.of(\"notes.txt\");\nFiles.writeString(path, \"Study loops\", StandardCharsets.UTF_8);",
  "blocks": [
    {
      "kind": "text",
      "body": "Files let data outlive one run of a program. Java separates a path, which names a location, from the operation that reads or writes it. Modern `Path` and `Files` methods keep simple text work compact."
    },
    {
      "kind": "code",
      "caption": "The same Path is used for writing and reading. UTF-8 makes the text encoding an explicit part of the file format.",
      "code": "Path path = Path.of(\"courses.txt\");\nFiles.writeString(path, \"CS101\\nMATH120\\n\", StandardCharsets.UTF_8);\nString text = Files.readString(path, StandardCharsets.UTF_8);\nSystem.out.print(text);",
      "output": "CS101\nMATH120"
    },
    {
      "kind": "text",
      "body": "These operations report IOException because disks, permissions, and paths can fail outside the program's control. Java requires the method to catch that checked exception or declare that it may pass upward."
    },
    {
      "kind": "compare",
      "caption": "The left side replaces existing text. The right side adds content at the end by passing the append option.",
      "wrong_label": "Replaces",
      "wrong": "Files.writeString(path, \"next\\n\");",
      "right_label": "Appends",
      "right": "Files.writeString(path, \"next\\n\",\n    StandardOpenOption.CREATE,\n    StandardOpenOption.APPEND);"
    },
    {
      "kind": "callout",
      "tone": "warning",
      "title": "Default writing replaces contents",
      "body": "A successful write can still be the wrong action. If old records matter, include the append option or read, update, and rewrite the complete data intentionally."
    },
    {
      "kind": "check",
      "prompt": "Which type represents a file-system path?",
      "code": "Path notes = Path.of(\"notes.txt\");",
      "choices": [
        "Path",
        "FileText",
        "Scanner",
        "StringBuilder"
      ],
      "answer_index": 0,
      "why": "`Path` represents a location and can be passed to Files operations. A String contains characters but does not provide the file-system abstraction."
    },
    {
      "kind": "text",
      "body": "For large files, `Files.lines` can process a stream instead of placing the whole file in one String. Use try-with-resources so the stream closes when processing ends."
    },
    {
      "kind": "check",
      "prompt": "Why use try-with-resources around `Files.lines(path)`?",
      "code": "try (Stream<String> lines = Files.lines(path)) {\n    lines.forEach(System.out::println);\n}",
      "choices": [
        "It closes the stream automatically",
        "It sorts every line",
        "It prevents IOException at compile time",
        "It converts the file to an array"
      ],
      "answer_index": 0,
      "why": "The stream holds an open file resource. Try-with-resources calls its close method even when processing throws an exception."
    },
    {
      "kind": "list",
      "title": "File I/O checklist",
      "items": [
        "Represent locations with Path.",
        "Select UTF-8 for portable text.",
        "Decide whether a write replaces or appends.",
        "Handle IOException at a layer that can respond.",
        "Stream large files and close the stream automatically."
      ]
    }
  ]
}
