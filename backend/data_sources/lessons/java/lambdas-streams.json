{
  "_note": "ORIGINAL content. Topic coverage follows David Eck's Introduction to Programming Using Java. No prose, examples, or exercises were copied; the source guides curriculum order only.",
  "title": "Lambdas & Streams",
  "summary": "Describe a small action and apply it across collection data.",
  "minutes": 11,
  "error_classes": [
    "syntax",
    "runtime",
    "wrong_answer"
  ],
  "refresher": "A lambda is a short **function** **value**, such as `name -> name.length()`. A stream processes collection data through steps like `filter`, `map`, and `sorted`, then finishes with an operation such as `toList`, `count`, or `forEach`. Stream steps do not change the original collection unless your code changes its **objects**.",
  "refresher_code": "List<String> shortNames = names.stream()\n    .filter(name -> name.length() < 5)\n    .toList();",
  "sections": [
    {
      "id": "idea",
      "title": "Describe collection steps",
      "summary": "Use streams when filtering, mapping, and collecting read like a pipeline.",
      "blocks": [
        {
          "kind": "text",
          "body": "Some collection work follows a clear sequence: keep certain items, change each one, then collect the results. **Streams** let the code state those steps in the same order."
        },
        {
          "kind": "visual",
          "title": "Stream data moves through a pipeline",
          "concept": "stream-flow",
          "caption": "Trace scores through filter, map, and the terminal operation that collects the result.",
          "initial_state": {},
          "steps": [
            {
              "title": "Start with collection",
              "body": "The original list supplies values to the stream, but the stream does not change that list.",
              "animation": "add",
              "state": {
                "active_step": "source",
                "process_flow": {
                  "steps": [
                    {"id": "source", "label": "Source", "detail": "scores = [72, 48, 90]", "shape": "terminator"},
                    {"id": "filter", "label": "Filter", "detail": "score >= 60", "shape": "diamond"},
                    {"id": "map", "label": "Map", "detail": "score + 5", "shape": "input"},
                    {"id": "terminal", "label": "Terminal", "detail": "toList asks for work", "shape": "card"},
                    {"id": "result", "label": "Result", "detail": "adjusted waits", "shape": "terminator"}
                  ]
                },
                "items": [72, 48, 90],
                "active": [0],
                "note": "the source collection stays the same"
              }
            },
            {
              "title": "Filter passing values",
              "body": "Filter keeps values whose condition is true and drops the others from later steps.",
              "animation": "highlight",
              "state": {
                "active_step": "filter",
                "process_flow": {
                  "steps": [
                    {"id": "source", "label": "Source", "detail": "read 48", "shape": "terminator"},
                    {"id": "filter", "label": "Filter", "detail": "48 >= 60 is false", "shape": "diamond"},
                    {"id": "map", "label": "Map", "detail": "skipped", "shape": "input"},
                    {"id": "terminal", "label": "Terminal", "detail": "keeps waiting", "shape": "card"},
                    {"id": "result", "label": "Result", "detail": "no 48 in output", "shape": "terminator"}
                  ]
                },
                "items": [72, 48, 90],
                "active": [1],
                "note": "a filtered-out value never reaches map"
              }
            },
            {
              "title": "Map kept values",
              "body": "Map changes each kept value into the value the result should store.",
              "animation": "slide",
              "state": {
                "active_step": "map",
                "process_flow": {
                  "steps": [
                    {"id": "source", "label": "Source", "detail": "read 90", "shape": "terminator"},
                    {"id": "filter", "label": "Filter", "detail": "90 passes", "shape": "diamond"},
                    {"id": "map", "label": "Map", "detail": "90 + 5 becomes 95", "shape": "input"},
                    {"id": "terminal", "label": "Terminal", "detail": "collect transformed value", "shape": "card"},
                    {"id": "result", "label": "Result", "detail": "building [77, 95]", "shape": "terminator"}
                  ]
                },
                "items": [72, 48, 90],
                "active": [2],
                "note": "map transforms values; it does not decide whether to keep them"
              }
            },
            {
              "title": "Collect the result",
              "body": "The terminal operation starts the pipeline work and gathers the final list.",
              "animation": "add",
              "state": {
                "active_step": "result",
                "process_flow": {
                  "steps": [
                    {"id": "source", "label": "Source", "detail": "all scores checked", "shape": "terminator"},
                    {"id": "filter", "label": "Filter", "detail": "kept 72 and 90", "shape": "diamond"},
                    {"id": "map", "label": "Map", "detail": "77 and 95", "shape": "input"},
                    {"id": "terminal", "label": "Terminal", "detail": "toList finishes", "shape": "card"},
                    {"id": "result", "label": "Result", "detail": "adjusted = [77, 95]", "shape": "terminator"}
                  ]
                },
                "items": [77, 95],
                "active": [0, 1],
                "note": "without a terminal operation, the pipeline only describes work"
              }
            }
          ]
        }
      ]
    },
    {
      "id": "examples",
      "title": "Read the pipeline",
      "summary": "Trace each step from input collection to final result.",
      "blocks": [
        {
          "kind": "code",
          "caption": "The filter keeps passing scores. The **map** adds five points to each remaining score. `toList` starts the work and gathers the results.",
          "code": "List<Integer> scores = List.of(72, 48, 90);\nList<Integer> adjusted = scores.stream()\n    .filter(score -> score >= 60)\n    .map(score -> score + 5)\n    .toList();\nSystem.out.println(adjusted);",
          "output": "[77, 95]"
        },
        {
          "kind": "text",
          "body": "The expression before the arrow names each **input**. The expression after it produces a result. Braces are needed when the lambda body has more than one statement."
        }
      ]
    },
    {
      "id": "pitfalls",
      "title": "Keep the pipeline simple",
      "summary": "Do not hide complicated state changes inside a lambda.",
      "blocks": [
        {
          "kind": "compare",
          "caption": "A stream needs a final operation. Without `toList`, `count`, or another terminal step, the earlier steps describe work but do not run it.",
          "wrong_label": "No result requested",
          "wrong": "scores.stream()\n    .filter(score -> score >= 60);",
          "right_label": "Result collected",
          "right": "List<Integer> passing = scores.stream()\n    .filter(score -> score >= 60)\n    .toList();"
        },
        {
          "kind": "callout",
          "tone": "tip",
          "title": "A loop may be clearer",
          "body": "Use a regular **loop** when the work needs several changing **variables**, early exits, detailed error handling, or many side effects. Streams fit best when data moves through a short **set** of clear steps."
        }
      ]
    },
    {
      "id": "check",
      "title": "Check stream behavior",
      "summary": "Practice filters, maps, and terminal operations.",
      "blocks": [
        {
          "kind": "check",
          "prompt": "Which stream operation keeps only items that pass a **condition**?",
          "code": "names.stream().____(name -> name.length() > 3)",
          "choices": [
            "filter",
            "map",
            "count",
            "toList"
          ],
          "answer_index": 0,
          "why": "Filter receives a **condition** and keeps **values** for which that condition is true. **Map** changes values instead of deciding whether to keep them."
        },
        {
          "kind": "text",
          "body": "**Map** changes every selected value into another value. The output type may differ from the input type, such as changing each **String** into an **Integer** length."
        },
        {
          "kind": "check",
          "prompt": "What does this stream produce?",
          "code": "List<String> names = List.of(\"Ada\", \"Grace\");\nList<Integer> lengths = names.stream()\n    .map(name -> name.length())\n    .toList();",
          "choices": [
            "[3, 5]",
            "[Ada, Grace]",
            "[true, true]",
            "8"
          ],
          "answer_index": 0,
          "why": "**Map** replaces each **String** with the **Integer** returned by `length()`. Ada has three characters and Grace has five."
        }
      ]
    },
    {
      "id": "practice",
      "title": "Stream habits",
      "summary": "Use a checklist before choosing streams.",
      "blocks": [
        {
          "kind": "list",
          "title": "Lambda and stream checklist",
          "items": [
            "Use a lambda for one small action.",
            "Read a stream from top to bottom as data steps.",
            "Use filter to keep and **map** to change.",
            "Finish with a terminal operation.",
            "Choose a **loop** when the stream would hide the real work."
          ]
        }
      ]
    }
  ],
  "learn_cards": {
    "java-lambdas-streams-check-1": {
      "title": "Choose the matching stream pipeline reasoning rule",
      "notice": "Separate filter, map, and terminal operations before deciding what the pipeline produces.",
      "mistake": "Do not keep a stream pipeline if a regular loop would make the changing state easier to see.",
      "section_id": "examples",
      "example": "scores.stream().filter(score -> score >= 60).count()",
      "source": "question",
      "body": "This question tests which stream operation keeps only items that pass a condition? A stream pipeline describes steps over collection data and only runs when a terminal operation asks for a result."
    },
    "java-lambdas-streams-check-2": {
      "title": "Stream pipeline reasoning",
      "notice": "Separate filter, map, and terminal operations before deciding what the pipeline produces.",
      "mistake": "Do not keep a stream pipeline if a regular loop would make the changing state easier to see.",
      "section_id": "examples",
      "example": "scores.stream().filter(score -> score >= 60).count()",
      "source": "question",
      "body": "This question tests what does this stream produce? A stream pipeline describes steps over collection data and only runs when a terminal operation asks for a result."
    },
    "java-lambdas-streams-output-01": {
      "title": "Trace stream pipeline reasoning output",
      "notice": "Write down each value at the moment it is printed, returned, or checked.",
      "mistake": "Do not jump to the final line first. Trace each assignment and output in order.",
      "section_id": "examples",
      "example": "scores.stream().filter(score -> score >= 60).count()",
      "source": "question",
      "body": "This question tests what output does this code produce? enter each line in order. A stream pipeline describes steps over collection data and only runs when a terminal operation asks for a result."
    },
    "java-lambdas-streams-supplement-01": {
      "title": "Filter keeps matching items",
      "notice": "filter keeps values that pass a condition and drops the rest.",
      "mistake": "Do not keep a stream pipeline if a regular loop would make the changing state easier to see.",
      "section_id": "check",
      "example": "scores.stream().filter(score -> score >= 60).count()",
      "source": "question",
      "body": "This question tests what is wrong with scores.stream().filter(score -> score >=... A stream pipeline describes steps over collection data and only runs when a terminal operation asks for a result."
    },
    "java-lambdas-streams-authored-01": {
      "title": "Stream pipeline reasoning",
      "notice": "Separate filter, map, and terminal operations before deciding what the pipeline produces.",
      "mistake": "Do not keep a stream pipeline if a regular loop would make the changing state easier to see.",
      "section_id": "idea",
      "example": "scores.stream().filter(score -> score >= 60).count()",
      "source": "question",
      "body": "This question tests when might a regular loop be clearer than a java stream pip... A stream pipeline describes steps over collection data and only runs when a terminal operation asks for a result."
    },
    "java-lambdas-streams-authored-02": {
      "title": "Stream pipeline reasoning",
      "notice": "Separate filter, map, and terminal operations before deciding what the pipeline produces.",
      "mistake": "Do not keep a stream pipeline if a regular loop would make the changing state easier to see.",
      "section_id": "idea",
      "example": "scores.stream().filter(score -> score >= 60).count()",
      "source": "question",
      "body": "This question tests what makes a lambda comfortable to read? A stream pipeline describes steps over collection data and only runs when a terminal operation asks for a result."
    },
    "java-lambdas-streams-authored-03": {
      "title": "Stream pipeline reasoning",
      "notice": "Separate filter, map, and terminal operations before deciding what the pipeline produces.",
      "mistake": "Do not keep a stream pipeline if a regular loop would make the changing state easier to see.",
      "section_id": "check",
      "example": "scores.stream().filter(score -> score >= 60).count()",
      "source": "question",
      "body": "This question tests how should a student mentally read a stream pipeline? A stream pipeline describes steps over collection data and only runs when a terminal operation asks for a result."
    },
    "java-lambdas-streams-authored-04": {
      "title": "Filter keeps matching items",
      "notice": "filter keeps values that pass a condition and drops the rest.",
      "mistake": "Do not keep a stream pipeline if a regular loop would make the changing state easier to see.",
      "section_id": "check",
      "example": "scores.stream().filter(score -> score >= 60).count()",
      "source": "question",
      "body": "This question tests how do filter and map divide their jobs? A stream pipeline describes steps over collection data and only runs when a terminal operation asks for a result."
    },
    "java-lambdas-streams-authored-05": {
      "title": "Terminal operation runs stream",
      "notice": "Without a terminal operation, the stream pipeline has not produced a final result.",
      "mistake": "Do not keep a stream pipeline if a regular loop would make the changing state easier to see.",
      "section_id": "check",
      "example": "scores.stream().filter(score -> score >= 60).count()",
      "source": "question",
      "body": "This question tests why does a stream pipeline need a terminal operation? A stream pipeline describes steps over collection data and only runs when a terminal operation asks for a result."
    },
    "java-lambdas-streams-authored-06": {
      "title": "Stream pipeline reasoning",
      "notice": "Separate filter, map, and terminal operations before deciding what the pipeline produces.",
      "mistake": "Do not keep a stream pipeline if a regular loop would make the changing state easier to see.",
      "section_id": "check",
      "example": "scores.stream().filter(score -> score >= 60).count()",
      "source": "question",
      "body": "This question tests what should happen when a stream hides the real work? A stream pipeline describes steps over collection data and only runs when a terminal operation asks for a result."
    }
  }
}
