{
  "_note": "ORIGINAL content. Topic coverage follows David Eck's Introduction to Programming Using Java. No prose, examples, or exercises were copied; the source guides curriculum order only.",
  "title": "Enums",
  "summary": "Represent a small fixed **set** of named choices.",
  "minutes": 9,
  "error_classes": [
    "syntax",
    "wrong_answer"
  ],
  "refresher": "An enum defines the complete **set** of values allowed for a type. Write the choices once, compare them by name, and use a switch when behavior depends on the chosen value. Enum constants are **objects**, so an enum may also have fields and **methods**.",
  "refresher_code": "enum Status { NEW, ACTIVE, COMPLETE }\nStatus current = Status.ACTIVE;",
  "sections": [
    {
      "id": "idea",
      "title": "Name a fixed set of choices",
      "summary": "Use an enum when only a few named values are valid.",
      "blocks": [
        {
          "kind": "text",
          "body": "An **enum** is useful when some **values** come from a short **list** that should not grow while the program runs. A course status may be planned, active, or complete. Plain **strings** allow spelling mistakes, but an enum allows only the declared choices."
        }
      ]
    },
    {
      "id": "examples",
      "title": "Compare named choices",
      "summary": "Trace enum values through branches and switches.",
      "blocks": [
        {
          "kind": "code",
          "caption": "The **variable** can hold one Status constant. Java checks the name during compilation.",
          "code": "enum Status {\n    PLANNED, ACTIVE, COMPLETE\n}\n\nStatus current = Status.ACTIVE;\nSystem.out.println(current);",
          "output": "ACTIVE"
        },
        {
          "kind": "text",
          "body": "Enum constants are written with the enum type and a dot. They can be compared with `==` because each constant is one fixed **object**."
        }
      ]
    },
    {
      "id": "pitfalls",
      "title": "Avoid spelling-based states",
      "summary": "Do not use loose strings when the choices are fixed.",
      "blocks": [
        {
          "kind": "compare",
          "caption": "**Strings** can contain an unnoticed spelling difference. The enum version is checked by the compiler and gives the value a clear type.",
          "wrong_label": "String can drift",
          "wrong": "String status = \"acitve\";",
          "right_label": "Fixed choice",
          "right": "Status status = Status.ACTIVE;"
        },
        {
          "kind": "callout",
          "tone": "warning",
          "title": "Changing text into an enum can fail",
          "body": "`Status.valueOf(text)` requires an exact constant name. **Input** such as `\"active\"` does not match `ACTIVE` unless you clean or convert it first."
        }
      ]
    },
    {
      "id": "check",
      "title": "Check enum behavior",
      "summary": "Practice constants, comparisons, and switch branches.",
      "blocks": [
        {
          "kind": "check",
          "prompt": "Which assignment is valid for this enum?",
          "code": "enum Level { LOW, MEDIUM, HIGH }",
          "choices": [
            "Level level = Level.MEDIUM;",
            "Level level = \"MEDIUM\";",
            "Level level = 2;",
            "Level level = new Level();"
          ],
          "answer_index": 0,
          "why": "An enum **variable** holds one of its declared constants. The constant is reached through the enum type, so Level.MEDIUM is the valid **value**."
        },
        {
          "kind": "text",
          "body": "A switch works well with enums because the choices are known. The compiler can help show when a new constant needs another case."
        },
        {
          "kind": "check",
          "prompt": "What prints?",
          "code": "Level level = Level.HIGH;\nswitch (level) {\n    case LOW -> System.out.println(\"1\");\n    case MEDIUM -> System.out.println(\"2\");\n    case HIGH -> System.out.println(\"3\");\n}",
          "choices": [
            "3",
            "HIGH",
            "2",
            "Nothing"
          ],
          "answer_index": 0,
          "why": "The **value** is Level.HIGH, so the HIGH case runs and prints the text 3. The other cases are skipped."
        }
      ]
    },
    {
      "id": "practice",
      "title": "Enum habits",
      "summary": "Use a checklist for fixed-choice data.",
      "blocks": [
        {
          "kind": "list",
          "title": "Enum checklist",
          "items": [
            "Use an enum for a fixed **set** of choices.",
            "Name constants clearly and consistently.",
            "Store the enum **value** instead of a matching **string**.",
            "Handle outside text before calling `valueOf`.",
            "Use switch when each constant needs different behavior."
          ]
        }
      ]
    }
  ],
  "learn_cards": {
    "java-enums-check-1": {
      "title": "Choose the matching enum choices rule",
      "notice": "Notice whether outside text needs to be cleaned before it becomes an enum value.",
      "mistake": "Do not replace a known fixed state with loose strings that can be misspelled.",
      "section_id": "examples",
      "example": "Status status = Status.ACTIVE;",
      "source": "question",
      "body": "This question tests which assignment is valid for this enum? An enum represents a fixed set of named choices that the compiler can check."
    },
    "java-enums-check-2": {
      "title": "Trace the enum choices result",
      "notice": "Follow the code in order and update the stored state before deciding what appears.",
      "mistake": "Do not replace a known fixed state with loose strings that can be misspelled.",
      "section_id": "examples",
      "example": "Status status = Status.ACTIVE;",
      "source": "question",
      "body": "This question tests what prints? An enum represents a fixed set of named choices that the compiler can check."
    },
    "java-enums-output-01": {
      "title": "Trace enum choices output",
      "notice": "Write down each value at the moment it is printed, returned, or checked.",
      "mistake": "Do not jump to the final line first. Trace each assignment and output in order.",
      "section_id": "examples",
      "example": "Status status = Status.ACTIVE;",
      "source": "question",
      "body": "This question tests what output does this code produce? enter each line in order. An enum represents a fixed set of named choices that the compiler can check."
    },
    "java-enums-supplement-01": {
      "title": "Explain the enum choices reason",
      "notice": "Notice whether outside text needs to be cleaned before it becomes an enum value.",
      "mistake": "Do not replace a known fixed state with loose strings that can be misspelled.",
      "section_id": "check",
      "example": "Status status = Status.ACTIVE;",
      "source": "question",
      "body": "This question tests why does status status = status.active; catch mistakes that... An enum represents a fixed set of named choices that the compiler can check."
    },
    "java-enums-authored-01": {
      "title": "Enum text must match exactly",
      "notice": "valueOf expects the enum constant name, so outside text usually needs validation first.",
      "mistake": "Do not replace a known fixed state with loose strings that can be misspelled.",
      "section_id": "idea",
      "example": "Status status = Status.ACTIVE;",
      "source": "question",
      "body": "This question tests why can status.valueof(text) fail for user input such as \"a... An enum represents a fixed set of named choices that the compiler can check."
    },
    "java-enums-authored-02": {
      "title": "Enum choices",
      "notice": "Notice whether outside text needs to be cleaned before it becomes an enum value.",
      "mistake": "Do not replace a known fixed state with loose strings that can be misspelled.",
      "section_id": "idea",
      "example": "Status status = Status.ACTIVE;",
      "source": "question",
      "body": "This question tests when is an enum a good fit? An enum represents a fixed set of named choices that the compiler can check."
    },
    "java-enums-authored-03": {
      "title": "Enum choices",
      "notice": "Notice whether outside text needs to be cleaned before it becomes an enum value.",
      "mistake": "Do not replace a known fixed state with loose strings that can be misspelled.",
      "section_id": "check",
      "example": "Status status = Status.ACTIVE;",
      "source": "question",
      "body": "This question tests what makes enum constants easier to use across a project? An enum represents a fixed set of named choices that the compiler can check."
    },
    "java-enums-authored-04": {
      "title": "Explain the enum choices reason",
      "notice": "Notice whether outside text needs to be cleaned before it becomes an enum value.",
      "mistake": "Do not replace a known fixed state with loose strings that can be misspelled.",
      "section_id": "check",
      "example": "Status status = Status.ACTIVE;",
      "source": "question",
      "body": "This question tests why store an enum value instead of a matching string when t... An enum represents a fixed set of named choices that the compiler can check."
    },
    "java-enums-authored-05": {
      "title": "Enum text must match exactly",
      "notice": "valueOf expects the enum constant name, so outside text usually needs validation first.",
      "mistake": "Do not replace a known fixed state with loose strings that can be misspelled.",
      "section_id": "check",
      "example": "Status status = Status.ACTIVE;",
      "source": "question",
      "body": "This question tests what should happen before outside text is passed to valueof? An enum represents a fixed set of named choices that the compiler can check."
    },
    "java-enums-authored-06": {
      "title": "Enum choices",
      "notice": "Notice whether outside text needs to be cleaned before it becomes an enum value.",
      "mistake": "Do not replace a known fixed state with loose strings that can be misspelled.",
      "section_id": "check",
      "example": "Status status = Status.ACTIVE;",
      "source": "question",
      "body": "This question tests when does a switch over enum constants make sense? An enum represents a fixed set of named choices that the compiler can check."
    }
  }
}
