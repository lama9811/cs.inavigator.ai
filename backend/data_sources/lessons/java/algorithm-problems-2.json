{
  "_note": "ORIGINAL CS Navigator content expanded into sections. No source prose, examples, or exercises were copied.",
  "title": "Algorithm Problems: Part 2",
  "summary": "Coordinate multiple state **variables**, define **edge**-case behavior, and trace updates deliberately.",
  "minutes": 16,
  "error_classes": [
    "wrong_answer",
    "runtime",
    "timeout"
  ],
  "refresher": "A multi-step Java algorithm may track a value and its **index**, a count and total, or a current answer and best answer. State what each **variable** means after every pass, then handle empty **input** and ties deliberately.",
  "refresher_code": "int best = 0;\nfor (int i = 1; i < values.length; i++) {\n    if (values[i] > values[best]) {\n        best = i;\n    }\n}",
  "sections": [
    {
      "id": "state-invariant",
      "title": "Give every variable a job",
      "summary": "Multi-**variable** algorithms work when each variable has a clear meaning.",
      "blocks": [
        {
          "kind": "text",
          "body": "Part 2 problems usually fail when **variables** stop agreeing with each other. A variable like `best` is not just an **integer**. It is a promise: after each **loop** pass, it stores the index of the best candidate seen so far."
        },
        {
          "kind": "visual",
          "title": "Track best-so-far state",
          "concept": "arrays",
          "caption": "Watch the best index update only when a better value appears.",
          "initial_state": {},
          "steps": [
            {
              "title": "Initialize",
              "body": "Best starts at index 0 because that is the first candidate.",
              "state": {
                "items": [
                  "4",
                  "9",
                  "7"
                ],
                "active": [
                  0
                ],
                "pointers": {
                  "best": 0
                },
                "note": "best value = 4"
              }
            },
            {
              "title": "Compare index 1",
              "body": "Nine is larger than four, so best moves to index 1.",
              "animation": "slide",
              "state": {
                "items": [
                  "4",
                  "9",
                  "7"
                ],
                "active": [
                  1
                ],
                "pointers": {
                  "best": 1,
                  "i": 1
                },
                "note": "best value = 9"
              }
            },
            {
              "title": "Compare index 2",
              "body": "Seven is not larger than nine, so best stays put.",
              "state": {
                "items": [
                  "4",
                  "9",
                  "7"
                ],
                "active": [
                  2
                ],
                "pointers": {
                  "best": 1,
                  "i": 2
                },
                "note": "return best index 1"
              }
            }
          ]
        },
        {
          "kind": "code",
          "caption": "Here `best` stores an **index**, not the largest value. That lets the **method** **return** where the largest value lives instead of only returning the value itself.",
          "code": "static int largestIndex(int[] values) {\n    if (values.length == 0) {\n        return -1;\n    }\n\n    int best = 0;\n    for (int i = 1; i < values.length; i++) {\n        if (values[i] > values[best]) {\n            best = i;\n        }\n    }\n\n    return best;\n}",
          "output": "Returns 1 for {4, 9, 7}"
        },
        {
          "kind": "callout",
          "tone": "tip",
          "title": "Say the invariant out loud",
          "body": "After the **loop** has checked through **index** `i`, `best` should be the index of the largest **value** in that checked section."
        },
        {
          "kind": "check",
          "prompt": "What does `best` represent in this **method**?",
          "code": "if (values[i] > values[best]) {\n    best = i;\n}",
          "choices": [
            "the index of the best value so far",
            "the biggest value itself",
            "the array length",
            "the number of loop passes"
          ],
          "answer_index": 0,
          "why": "The code compares with `values[best]` and later returns `best`, so `best` is an **index**."
        }
      ]
    },
    {
      "id": "edges-and-ties",
      "title": "Decide empty input and ties before coding",
      "summary": "**Edge cases** are part of the algorithm, not cleanup afterward.",
      "blocks": [
        {
          "kind": "text",
          "body": "A common habit is to write the main **loop** first and then discover that empty **arrays**, one-item arrays, or tied **values** do not fit. Reverse that order. Before the loop, decide what answer makes sense when there is no candidate and what should happen when two candidates are equal."
        },
        {
          "kind": "compare",
          "wrong_label": "Buggy shape",
          "wrong": "static int largestIndex(int[] values) {\n    int best = 0;\n    for (int i = 0; i < values.length; i++) {\n        if (values[i] > values[best]) {\n            best = i;\n        }\n    }\n    return best;\n}",
          "right_label": "Guarded shape",
          "right": "static int largestIndex(int[] values) {\n    if (values.length == 0) return -1;\n    int best = 0;\n    for (int i = 1; i < values.length; i++) {\n        if (values[i] > values[best]) {\n            best = i;\n        }\n    }\n    return best;\n}",
          "caption": "The buggy version crashes on an empty **array** before the **loop** can prove anything useful. The guarded version names the no-candidate answer first."
        },
        {
          "kind": "check",
          "prompt": "With strict `>`, which tied maximum **index** remains?",
          "code": "int[] values = {8, 3, 8};",
          "choices": [
            "the first maximum index",
            "the last maximum index",
            "both indexes",
            "no result"
          ],
          "answer_index": 0,
          "why": "Equal **values** do not pass a strict greater-than check, so the first maximum keeps its spot."
        }
      ]
    },
    {
      "id": "nested-state",
      "title": "Separate row state from whole-answer state",
      "summary": "Nested loops often need one variable for the current group and another for the answer so far.",
      "blocks": [
        {
          "kind": "text",
          "body": "In Java matrix or table problems, a variable declared inside the outer loop restarts for each row. A variable declared before the outer loop can keep the best result across all rows."
        },
        {
          "kind": "compare",
          "caption": "Reset row-local state at the row boundary, not after the whole grid.",
          "wrong_label": "Leaky row state",
          "wrong": "int rowTotal = 0;\nfor (int[] row : grid) {\n    for (int value : row) {\n        rowTotal += value;\n    }\n}",
          "right_label": "Fresh row state",
          "right": "for (int[] row : grid) {\n    int rowTotal = 0;\n    for (int value : row) {\n        rowTotal += value;\n    }\n}"
        },
        {
          "kind": "check",
          "prompt": "Where should `int rowTotal = 0;` go when each row needs its own total?",
          "choices": [
            "Inside the outer loop before the inner loop",
            "After all loops finish",
            "Inside the inner loop after each value",
            "Inside the return statement"
          ],
          "answer_index": 0,
          "why": "Declaring the row total inside the outer loop creates a fresh total for each row."
        }
      ]
    },
    {
      "id": "trace-table",
      "title": "Trace the first wrong update",
      "summary": "A small table can reveal whether the **value**, **index**, or **loop** range is wrong.",
      "blocks": [
        {
          "kind": "code",
          "caption": "When an algorithm feels tangled, **print** the **loop** **index**, the current value, and the tracked state together.",
          "code": "for (int i = 1; i < values.length; i++) {\n    System.out.println(\"i=\" + i + \" value=\" + values[i] + \" best=\" + best);\n    if (values[i] > values[best]) {\n        best = i;\n    }\n}",
          "output": "i=1 value=9 best=0\ni=2 value=7 best=1"
        },
        {
          "kind": "list",
          "title": "Trace questions",
          "items": [
            "What does each **variable** mean before the **loop** starts?",
            "Which **input** is the smallest valid input?",
            "Does equality replace the current answer?",
            "Which printed line first disagrees with the promise?"
          ]
        }
      ]
    },
    {
      "id": "parsons-ordering",
      "title": "Order setup, loop body, and return",
      "summary": "Parsons problems test where each line belongs in the method shape.",
      "blocks": [
        {
          "kind": "text",
          "body": "A Java algorithm usually prepares variables, enters a loop, updates state inside the loop, and returns after the loop. Braces make that placement visible."
        },
        {
          "kind": "list",
          "title": "Line-order checks",
          "items": [
            "Variable setup happens before the repeated work.",
            "State updates happen inside the loop that sees the data.",
            "A nested update belongs inside the inner loop.",
            "The final return happens after the loop has finished."
          ]
        },
        {
          "kind": "check",
          "prompt": "Which line usually belongs before a loop that counts matches?",
          "code": "",
          "choices": [
            "int count = 0;",
            "return count;",
            "count++;",
            "System.out.println(value);"
          ],
          "answer_index": 0,
          "why": "The counter needs a starting value before the loop can add matches to it."
        }
      ]
    }
  ],
  "learn_cards": {
    "algo-p2-01": {
      "title": "Sorted flag invariant",
      "body": "This algorithm keeps a flag that means no out-of-order pair has been found yet. Each pass compares a value with the value just before it.",
      "notice": "The loop starts after the first item because each comparison looks backward one position.",
      "mistake": "Do not compare a value with itself or start at a position with no previous neighbor.",
      "section_id": "state-invariant",
      "example": "current vs previous",
      "source": "question"
    },
    "algo-p2-02": {
      "title": "Running totals",
      "body": "Running totals keep one ongoing total and record a snapshot after each addition. The saved collection grows after the current value is included.",
      "notice": "Add first, then store the new total for that position.",
      "mistake": "Do not reset the total each pass; only the snapshot changes each pass.",
      "section_id": "state-invariant",
      "example": "total grows, result records",
      "source": "question"
    },
    "algo-p2-03": {
      "title": "Nested matrix scan",
      "body": "A matrix count needs two loops: one to choose each row and one to inspect each value in that row. The counter changes only on a target match.",
      "notice": "The inner loop must be nested inside the row loop.",
      "mistake": "Do not increment once per row when the prompt asks for matching cells.",
      "section_id": "trace-table",
      "example": "row -> value -> match?",
      "source": "question"
    },
    "algo-p2-04": {
      "title": "Two best values",
      "body": "Tracking the largest and second-largest distinct values means the variables depend on each other. When a new largest appears, the old largest has to move into second place.",
      "notice": "Update second before replacing largest with the new value.",
      "mistake": "Do not let a duplicate of the largest count as the second-largest distinct value.",
      "section_id": "state-invariant",
      "example": "old largest becomes second",
      "source": "question"
    },
    "algo-p2-05": {
      "title": "First occurrences",
      "body": "This pattern preserves order by scanning left to right and adding a value only if it has not already been kept.",
      "notice": "The membership check happens before the append or add step.",
      "mistake": "Do not sort the values or use a plain set if the prompt asks to keep first-seen order.",
      "section_id": "edges-and-ties",
      "example": "seen before? then skip",
      "source": "question"
    },
    "algo-p2-06": {
      "title": "Mirrored string positions",
      "body": "A palindrome check compares matching positions from the front and back. One mismatch is enough to change the answer flag.",
      "notice": "Only half the string needs direct comparisons.",
      "mistake": "Do not compare every character with the same final character.",
      "section_id": "edges-and-ties",
      "example": "left i, right length - i - 1",
      "source": "question"
    },
    "algo-p2-07": {
      "title": "Reset row-local state",
      "body": "A best-row algorithm has two levels of state: a row total that restarts for each row and a best value that survives across rows.",
      "notice": "Reset the row total inside the outer loop but before the inner loop.",
      "mistake": "Do not reset the best value for every row.",
      "section_id": "trace-table",
      "example": "row total resets, best survives",
      "source": "question"
    },
    "algo-p2-08": {
      "title": "Separate kept values and zeros",
      "body": "Moving zeros to the end can be traced as two pieces of state: the kept nonzero values and the number of zeros seen so far.",
      "notice": "The scan preserves nonzero order first, then appends the counted zeros.",
      "mistake": "Do not append zeros immediately if the prompt wants them all at the end.",
      "section_id": "trace-table",
      "example": "keep nonzeros, count zeros",
      "source": "question"
    }
  }
}
