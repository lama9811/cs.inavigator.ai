{
  "_note": "ORIGINAL content. Think C++ and the Google C++ Class outline informed coverage only; no prose, examples, or exercises were copied.",
  "title": "Conditionals",
  "summary": "Choose which code runs by asking true-or-false questions.",
  "minutes": 10,
  "error_classes": [
    "wrong_answer",
    "syntax"
  ],
  "refresher": "C++ checks `if`, then any `else if` **branches**, and finally `else`. Only the first true branch runs. Put braces around each branch so its beginning and end are easy to see.",
  "refresher_code": "if(score>=90){ grade=\"A\"; }\nelse { grade=\"Below A\"; }",
  "sections": [
    {
      "id": "if-shape",
      "title": "The if statement shape",
      "summary": "Use comparisons and braces to make the branch body clear.",
      "blocks": [
        {
          "kind": "text",
          "body": "A **conditional** lets the program choose. C++ tests the expression inside `if (...)`. If it is true, that block runs. Otherwise, the program tries the next `else if` or uses the final `else`."
        },
        {
          "kind": "code",
          "caption": "Before real syntax, learn the shape: ask a condition, then run only the matching **body**. The examples after this show the exact punctuation for this language.",
          "code": "if condition is true:\n    body for the true case\notherwise:\n    body for the backup case"
        },
        {
          "kind": "text",
          "body": "Write comparisons that clearly show the rule, such as `score >= 60`. Put the most specific overlapping rule first because only the first true branch runs. Braces make it clear which statements belong to each choice."
        },
        {
          "kind": "callout",
          "tone": "tip",
          "title": "Braces do the job indentation does in Python",
          "body": "Python uses indentation to show which statements belong to an `if`. C++ uses braces. Indentation still helps people read C++, but the braces are what the compiler follows."
        },
        {
          "kind": "code",
          "caption": "A score of 92 satisfies the first **condition**, so C++ prints A and skips the remaining **branches**.",
          "code": "#include <iostream>\n\nint main() {\n  int score = 92;\n\n  if (score >= 90) {\n    std::cout << \"A\\n\";\n  } else if (score >= 80) {\n    std::cout << \"B\\n\";\n  } else {\n    std::cout << \"C or below\\n\";\n  }\n}",
          "output": "A"
        }
      ]
    },
    {
      "id": "semicolon-trap",
      "title": "The semicolon trap",
      "summary": "Keep the if line attached to the block that should be controlled.",
      "blocks": [
        {
          "kind": "compare",
          "caption": "Remove the stray semicolon so the block belongs to the **condition**.",
          "wrong_label": "Common mistake",
          "wrong": "if (score >= 60); {\n  passed = true;\n}",
          "right_label": "Clearer version",
          "right": "if (score >= 60) {\n  passed = true;\n}"
        },
        {
          "kind": "text",
          "body": "The semicolon in the first version finishes the `if` immediately. The block below it is separate and runs every time. Remove that semicolon so the block runs only when the score is at least sixty."
        },
        {
          "kind": "callout",
          "tone": "mistake",
          "title": "Do not end the if line with a semicolon",
          "body": "Write `if (condition) {`, not `if (condition); {`. That extra semicolon creates an empty branch and makes the following block run on its own."
        }
      ]
    },
    {
      "id": "check",
      "title": "Check branch reasoning",
      "summary": "Practice tracing which branch runs.",
      "blocks": [
        {
          "kind": "check",
          "prompt": "What is printed?",
          "code": "int n=0;\nif(n) std::cout<<\"yes\"; else std::cout<<\"no\";",
          "choices": [
            "no",
            "yes",
            "0",
            "an error"
          ],
          "answer_index": 0,
          "why": "Zero converts to false in the **condition**, so the else branch runs."
        },
        {
          "kind": "check",
          "prompt": "Why test 90 before 60?",
          "code": "",
          "choices": [
            "95 satisfies both thresholds",
            "C++ sorts conditions",
            "else requires equality",
            "it changes score"
          ],
          "answer_index": 0,
          "why": "Only the first true branch runs, so the broader rule would hide the narrower grade."
        }
      ]
    },
    {
      "id": "habits",
      "title": "Conditional habits",
      "summary": "Use the checklist before coding conditionals.",
      "blocks": [
        {
          "kind": "list",
          "title": "Conditionals checklist",
          "items": [
            "Order overlapping rules.",
            "Use visible braces.",
            "Remove stray semicolons.",
            "Test each boundary."
          ]
        }
      ]
    }
  ],
  "_beginner_expanded": true,
  "learn_cards": {
    "if-else-01": {
      "title": "If and else choose one path",
      "body": "An if/else pair chooses between two alternatives. If the condition is true, the if body runs; otherwise the else body runs.",
      "example": "if condition:\n    do_this\nelse:\n    do_that",
      "notice": "Only one branch in the pair should run.",
      "mistake": "Do not expect the else branch to run after a true if branch.",
      "section_id": "check"
    },
    "chain-first-true-01": {
      "title": "A chain stops at the first true branch",
      "body": "In an if/else-if chain, the program checks from top to bottom and runs the first branch whose condition is true.",
      "example": "if high:\n    ...\nelif medium:\n    ...",
      "notice": "Later branches are skipped once an earlier branch wins.",
      "mistake": "Do not choose a later true branch when an earlier true branch already matched.",
      "section_id": "check"
    },
    "separate-ifs-01": {
      "title": "Separate if statements are independent",
      "body": "Two separate if statements each ask their own question. Both can run if both conditions are true.",
      "example": "if first:\n    ...\nif second:\n    ...",
      "notice": "Separate if statements are not alternatives unless they are connected with else-if or else.",
      "mistake": "Do not treat separate ifs like a single chain.",
      "section_id": "check"
    },
    "assign-vs-compare-01": {
      "title": "Conditions compare, assignments store",
      "body": "A condition should produce true or false. Use comparison when asking whether a value matches something.",
      "example": "score == 100",
      "notice": "Assignment and comparison are different jobs.",
      "mistake": "Do not use assignment syntax when the condition should be a yes/no test.",
      "section_id": "check"
    },
    "and-or-01": {
      "title": "And means both; or means either",
      "body": "Use and when every rule must be true. Use or when one matching rule is enough.",
      "example": "age >= 13 and age <= 19",
      "notice": "Try a value outside the range to test whether the condition is too loose.",
      "mistake": "Do not use or for a between rule unless either side really should pass.",
      "section_id": "check"
    },
    "body-scope-01": {
      "title": "Only the branch body is controlled",
      "body": "A conditional controls the statements inside its body. Code after the branch runs normally unless another condition stops it.",
      "example": "if ready:\n    print(\"inside\")\nprint(\"after\")",
      "notice": "Indentation or braces decide what belongs to the branch.",
      "mistake": "Do not assume every line nearby is controlled by the if.",
      "section_id": "check"
    },
    "chain-order-01": {
      "title": "Order matters in condition chains",
      "body": "Put the most specific or highest-priority condition before a broader one when only one branch should run.",
      "example": "if score >= 90:\n    grade = \"A\"",
      "notice": "A broad condition can catch a value before a later, more specific condition gets a chance.",
      "mistake": "Do not place a general true condition above the branch you actually wanted.",
      "section_id": "check"
    },
    "not-operator-01": {
      "title": "Not flips a condition",
      "body": "The not operator changes true to false and false to true. It is useful when the branch should run for the opposite case.",
      "example": "if not finished:",
      "notice": "Read the condition slowly with the word not included.",
      "mistake": "Do not ignore not when predicting which branch runs.",
      "section_id": "check"
    },
    "nested-if-01": {
      "title": "Nested conditions need the outer branch first",
      "body": "An inner if is checked only after the outer if lets the program enter that block.",
      "example": "if logged_in:\n    if is_admin:\n        ...",
      "notice": "Trace the outer condition before deciding whether the inner one matters.",
      "mistake": "Do not jump to the inner branch when the outer condition is false.",
      "section_id": "check"
    },
    "boundary-01": {
      "title": "Boundaries decide edge cases",
      "body": "When a rule mentions values at the edge, check whether the comparison includes or excludes that exact value.",
      "example": "score >= 70",
      "notice": "`>=` and `<=` include the boundary. `>` and `<` exclude it.",
      "mistake": "Do not test only middle values; edge values reveal boundary mistakes.",
      "section_id": "check"
    }
  }
}
