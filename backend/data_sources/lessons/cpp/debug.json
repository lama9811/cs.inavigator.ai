{
  "_note": "ORIGINAL content. Think C++ and the Google C++ Class outline informed coverage only; no prose, examples, or exercises were copied.",
  "title": "Debug: Part 1",
  "summary": "Tell compile errors, crashes, wrong answers, and endless **loops** apart.",
  "minutes": 10,
  "error_classes": [
    "syntax",
    "runtime",
    "wrong_answer",
    "timeout"
  ],
  "refresher": "A compile error stops the program from being built. A **runtime error** happens after it starts. A **logic error** finishes with the wrong answer, and a timeout often means a **loop** did not stop. Identify the category before changing code.",
  "refresher_code": "std::vector<int> v{1,2};\nstd::cout << v.at(v.size()-1);",
  "sections": [
    {
      "id": "classify",
      "title": "Classify the bug family",
      "summary": "Separate compile errors, runtime failures, and wrong answers.",
      "blocks": [
        {
          "kind": "text",
          "body": "Debugging begins by naming what happened. Did the compiler reject the code? Did the program start and then fail? Did it finish with the wrong **output**? Did it keep running? Each symptom points you toward a different part of the program."
        },
        {
          "kind": "text",
          "body": "For a compile error, begin with the first compiler message. For a crash or range error, inspect **indexes** and empty collections. For wrong output, trace the values one step at a time. For a hang, inspect the **loop** **condition** and its update."
        },
        {
          "kind": "code",
          "caption": "The vector is not empty, so size minus one gives the final valid **index** and at checks the access.",
          "code": "#include <iostream>\n#include <vector>\n\nint main() {\n  std::vector<int> values{1, 2};\n\n  if (!values.empty()) {\n    std::cout << values.at(values.size() - 1) << \"\\n\";\n  }\n}",
          "output": "2"
        }
      ]
    },
    {
      "id": "bounds",
      "title": "Find boundary mistakes",
      "summary": "Use checked access to locate invalid indexes.",
      "blocks": [
        {
          "kind": "compare",
          "caption": "Prefer checked access while locating an **index** failure.",
          "wrong_label": "Common mistake",
          "wrong": "std::cout << values[values.size()];",
          "right_label": "Clearer version",
          "right": "std::cout << values.at(values.size() - 1);"
        },
        {
          "kind": "text",
          "body": "The **index** in the first version equals the element count, which is one position too far. The corrected expression uses the last valid position, but it still needs an empty check before subtracting one. Checked access gives a useful error instead of silently reading outside the vector."
        },
        {
          "kind": "callout",
          "tone": "mistake",
          "title": "Fix one clear cause at a time",
          "body": "One missing symbol can create several compiler messages. Start at the first reported location, make one focused change, rebuild, and then see which messages remain."
        }
      ]
    },
    {
      "id": "check",
      "title": "Check debug reasoning",
      "summary": "Practice classifying symptoms and checked-access behavior.",
      "blocks": [
        {
          "kind": "check",
          "prompt": "What kind of issue is a missing semicolon?",
          "code": "",
          "choices": [
            "compile error",
            "logic error",
            "timeout",
            "valid syntax"
          ],
          "answer_index": 0,
          "why": "The compiler cannot parse the statement boundary, so no executable is produced."
        },
        {
          "kind": "check",
          "prompt": "What does `.at(2)` do for two elements?",
          "code": "",
          "choices": [
            "throws out_of_range",
            "returns the last item",
            "adds an item",
            "returns zero"
          ],
          "answer_index": 0,
          "why": "The valid positions are zero and one, and checked access reports position two as out of range."
        }
      ]
    },
    {
      "id": "habits",
      "title": "Debug habits",
      "summary": "Use the checklist before making random edits.",
      "blocks": [
        {
          "kind": "list",
          "title": "Debug: Part 1 checklist",
          "items": [
            "Reproduce one **input**.",
            "Fix first diagnostics first.",
            "Inspect the named line.",
            "Retest after one change."
          ]
        }
      ]
    }
  ],
  "_beginner_expanded": true,
  "learn_cards": {
    "debug-01": {
      "title": "Assignment can erase a count",
      "body": "A counter should move from old value to new value. Setting it to a constant each pass loses the running count.",
      "notice": "Look for whether the variable accumulates or gets overwritten.",
      "mistake": "Do not call a loop correct just because it visits every item.",
      "section_id": "check",
      "example": "total += 1"
    },
    "debug-02": {
      "title": "Compare when asking a question",
      "body": "A condition should test a value, not assign a value by accident.",
      "notice": "Check equality syntax anywhere an `if` is asking yes or no.",
      "mistake": "Do not use assignment syntax when the branch needs a comparison.",
      "section_id": "check",
      "example": "x == 10"
    },
    "debug-03": {
      "title": "Start index controls skipped items",
      "body": "An index loop that starts at 1 skips the item at index 0.",
      "notice": "For scan-all code, verify the first and last indexes visited.",
      "mistake": "Do not assume `len` is wrong when the start boundary is the real issue.",
      "section_id": "check",
      "example": "0, 1, 2"
    },
    "debug-04": {
      "title": "Syntax errors often come from one missing closer",
      "body": "A line can fail before logic runs because a parenthesis, bracket, quote, or semicolon is missing.",
      "notice": "Match each opener with its closer before changing the algorithm.",
      "mistake": "Do not debug variable values when the parser cannot read the line.",
      "section_id": "check",
      "example": "( ... )"
    },
    "debug-05": {
      "title": "An expression must be stored to matter",
      "body": "Calculating `total + n` alone does not update total. The result must be assigned or returned.",
      "notice": "Find the line where state should change and check whether it actually writes the new value.",
      "mistake": "Do not mistake a calculation for an update.",
      "section_id": "check",
      "example": "total = total + n"
    },
    "debug-06": {
      "title": "Indexes stop before length",
      "body": "The last valid index is one less than the collection length.",
      "notice": "For out-of-range errors, compare the requested index with the valid index list.",
      "mistake": "Do not use the length itself as a valid index.",
      "section_id": "check",
      "example": "length 3 -> indexes 0, 1, 2"
    },
    "debug-07": {
      "title": "Return can end a search too early",
      "body": "A return inside a loop stops the whole function, not just the current pass.",
      "notice": "For search loops, put the failure return after the loop has checked every item.",
      "mistake": "Do not return false after the first non-match unless only the first item matters.",
      "section_id": "check",
      "example": "return after loop"
    },
    "debug-p1-08": {
      "title": "One extra index is still out of range",
      "body": "Looping through length plus one allows an index equal to the length, which is outside the collection.",
      "notice": "Check the final loop value before checking the collection access.",
      "mistake": "Do not include the length as an index in zero-based collections.",
      "section_id": "check",
      "example": "i < length"
    }
  }
}
