{
  "_note": "ORIGINAL content. Topic coverage follows the curriculum in Runestone Foundations of Python Programming, Think Python, and OpenStax Introduction to Python Programming. No prose, examples, or exercises were copied; the sources guide topic selection only.",
  "title": "Modules & Imports",
  "summary": "Split a program into **files** with clear jobs.",
  "minutes": 9,
  "error_classes": [
    "runtime",
    "syntax",
    "wrong_answer"
  ],
  "refresher": "A Python **file** is a **module**. Use `import module` to keep its name visible, or `from module import name` to bring in one item. Put code that should run only when a file starts the program under `if __name__ == \"__main__\":`.",
  "refresher_code": "import math\nprint(math.sqrt(25))\n# 5.0",
  "blocks": [
    {
      "kind": "text",
      "body": "One large file becomes difficult to scan. **Modules** let you place related **functions** and **classes** in smaller files. A grade calculator can live in one module while the main program handles input and output."
    },
    {
      "kind": "code",
      "caption": "Importing the **module** keeps the source of `sqrt` clear. The dot says that the **function** comes from math.",
      "code": "import math\n\nside = 5\nprint(math.sqrt(side * side))",
      "output": "5.0"
    },
    {
      "kind": "text",
      "body": "Python loads a **module** the first time it is imported during a run. It creates the module's names and makes them available to the importing **file**."
    },
    {
      "kind": "compare",
      "caption": "A wildcard **import** hides where names came from and can replace an existing name. Importing the **module** keeps that boundary visible.",
      "wrong_label": "Names are unclear",
      "wrong": "from math import *\nprint(sqrt(16))",
      "right_label": "Source is clear",
      "right": "import math\nprint(math.sqrt(16))"
    },
    {
      "kind": "callout",
      "tone": "warning",
      "title": "Imported files can run code",
      "body": "Statements at the top level run when Python **imports** the **file**. Place demo code and direct user input inside a main guard so importing the **module** does not start them."
    },
    {
      "kind": "check",
      "prompt": "Which line makes the `random` **module** available under its own name?",
      "code": "",
      "choices": [
        "import random",
        "from random import randint",
        "include random",
        "using random"
      ],
      "answer_index": 0,
      "why": "`import random` binds the **module** name. Its **functions** are then reached with a dot, such as `random.randint(1, 6)`."
    },
    {
      "kind": "text",
      "body": "The main guard checks how the **file** was started. Its block runs during direct execution, but not when another file **imports** the **module**."
    },
    {
      "kind": "check",
      "prompt": "When does this **print**?",
      "code": "if __name__ == \"__main__\":\n    print(\"Run demo\")",
      "choices": [
        "When this file is run directly",
        "Every time the file is imported",
        "Only after an error",
        "Before Python reads the file"
      ],
      "answer_index": 0,
      "why": "Python **sets** `__name__` to `\"__main__\"` for the **file** that starts the program. Imported **modules** receive their module names instead."
    },
    {
      "kind": "list",
      "title": "Module checklist",
      "items": [
        "Group code by one clear purpose.",
        "Prefer **imports** that show where a name came from.",
        "Avoid wildcard **imports**.",
        "Keep top-level work small.",
        "Put demos and command-line **input** behind a main guard."
      ]
    }
  ],
  "learn_cards": {
    "python-modules-imports-check-1": {
      "title": "Choose the matching import boundary rule",
      "notice": "Check whether the question is about import style, top-level code, or the main guard.",
      "mistake": "Do not use star imports when clear module names would prevent collisions and confusion.",
      "section_id": "check",
      "example": "import math\nmath.sqrt(16)",
      "source": "question",
      "body": "This question tests which line makes the random module available under its own... A module should expose reusable names while avoiding surprise work when another file imports it."
    },
    "python-modules-imports-check-2": {
      "title": "Trace the import boundary result",
      "notice": "Follow the code in order and update the stored state before deciding what appears.",
      "mistake": "Do not use star imports when clear module names would prevent collisions and confusion.",
      "section_id": "examples",
      "example": "import math\nmath.sqrt(16)",
      "source": "question",
      "body": "This question tests when does this print? A module should expose reusable names while avoiding surprise work when another file imports it."
    },
    "python-modules-imports-output-01": {
      "title": "Trace import boundary output",
      "notice": "Write down each value at the moment it is printed, returned, or checked.",
      "mistake": "Do not jump to the final line first. Trace each assignment and output in order.",
      "section_id": "examples",
      "example": "import math\nmath.sqrt(16)",
      "source": "question",
      "body": "This question tests what output does this code produce? enter each line in order. A module should expose reusable names while avoiding surprise work when another file imports it."
    },
    "python-modules-imports-supplement-01": {
      "title": "Avoid star imports",
      "notice": "Star imports hide where names came from and can overwrite names by accident.",
      "mistake": "Do not use star imports when clear module names would prevent collisions and confusion.",
      "section_id": "check",
      "example": "import math\nmath.sqrt(16)",
      "source": "question",
      "body": "This question tests why prefer import math; math.sqrt(16) over from math import *? A module should expose reusable names while avoiding surprise work when another file imports it."
    },
    "python-modules-imports-authored-01": {
      "title": "Predict the import boundary behavior",
      "notice": "Check whether the question is about import style, top-level code, or the main guard.",
      "mistake": "Do not use star imports when clear module names would prevent collisions and confusion.",
      "section_id": "idea",
      "example": "import math\nmath.sqrt(16)",
      "source": "question",
      "body": "This question tests what happens to top-level statements in a python file when... A module should expose reusable names while avoiding surprise work when another file imports it."
    },
    "python-modules-imports-authored-02": {
      "title": "Import boundary",
      "notice": "Check whether the question is about import style, top-level code, or the main guard.",
      "mistake": "Do not use star imports when clear module names would prevent collisions and confusion.",
      "section_id": "idea",
      "example": "import math\nmath.sqrt(16)",
      "source": "question",
      "body": "This question tests a file named grade_utils.py is growing. what should guide w... A module should expose reusable names while avoiding surprise work when another file imports it."
    },
    "python-modules-imports-authored-03": {
      "title": "Avoid star imports",
      "notice": "Star imports hide where names came from and can overwrite names by accident.",
      "mistake": "Do not use star imports when clear module names would prevent collisions and confusion.",
      "section_id": "check",
      "example": "import math\nmath.sqrt(16)",
      "source": "question",
      "body": "This question tests why can import math followed by math.sqrt(16) be clearer th... A module should expose reusable names while avoiding surprise work when another file imports it."
    },
    "python-modules-imports-authored-04": {
      "title": "Import boundary",
      "notice": "Check whether the question is about import style, top-level code, or the main guard.",
      "mistake": "Do not use star imports when clear module names would prevent collisions and confusion.",
      "section_id": "check",
      "example": "import math\nmath.sqrt(16)",
      "source": "question",
      "body": "This question tests what is a common problem with from module import *? A module should expose reusable names while avoiding surprise work when another file imports it."
    },
    "python-modules-imports-authored-05": {
      "title": "Import boundary",
      "notice": "Check whether the question is about import style, top-level code, or the main guard.",
      "mistake": "Do not use star imports when clear module names would prevent collisions and confusion.",
      "section_id": "check",
      "example": "import math\nmath.sqrt(16)",
      "source": "question",
      "body": "This question tests what kind of work is safest to leave at the top level of an... A module should expose reusable names while avoiding surprise work when another file imports it."
    },
    "python-modules-imports-authored-06": {
      "title": "Main guard protects imports",
      "notice": "The main guard keeps demo code from running just because another file imported the module.",
      "mistake": "Do not use star imports when clear module names would prevent collisions and confusion.",
      "section_id": "check",
      "example": "import math\nmath.sqrt(16)",
      "source": "question",
      "body": "This question tests why use if __name__ == \"__main__\": around demo code? A module should expose reusable names while avoiding surprise work when another file imports it."
    }
  }
}
